const Discord = require('discord.js');

module.exports = {
	name: 'mu',
  description: 'MessageEmbed Generate Random Map and Teams.',
	execute(message, args) {


    // message.member.voice.channel.members.forEach(member => {
      
    //   member.setVoiceChannel(697020450450047017).catch(console.error)

    // });

    //console.log(message.member)

    // member.setVoiceChannel('697020450450047017').catch(console.error);

    //console.log(message.channel.members);
    //console.log(message.member);

    // vs = new Discord.VoiceState(message, args);

    // console.log(vs);

    // message.member.setChannel();

	}
};